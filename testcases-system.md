# TEST CASE HỆ THỐNG QUẢN LÝ TIẾN ĐỘ THỰC HIỆN NHIỆM VỤ

Tài liệu này mô tả các test case kiểm thử thủ công phục vụ báo cáo đồ án cho hệ thống quản lý tiến độ thực hiện nhiệm vụ của Đảng ủy phường Tam Thanh. Các test case không phải kịch bản Jest/Mocha, không gọi API thật và dùng để đối chiếu khi kiểm thử chức năng trên giao diện hoặc môi trường kiểm thử.

## 1. Test case nhóm Authentication & Security

| Mã test case | Use Case liên quan | Mục tiêu kiểm thử | Tác nhân thực hiện | Điều kiện tiên quyết | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|
| TC_AUTH_01 | Đăng nhập | Kiểm tra người dùng có thể đăng nhập thành công bằng tài khoản hợp lệ và nhận thông tin phiên làm việc. | Lãnh đạo Đảng ủy | Tài khoản `lanhdao01` đã tồn tại, đang hoạt động và có vai trò `lanh_dao`. | username: `lanhdao01`<br>password: `Ld@123456` | 1. Mở trang đăng nhập tại `http://localhost:5173`.<br>2. Nhập username và password hợp lệ.<br>3. Nhấn nút đăng nhập.<br>4. Quan sát màn hình sau đăng nhập. | Hệ thống đăng nhập thành công, tạo phiên JWT hợp lệ, hiển thị thông tin người dùng `lanhdao01` và điều hướng đến dashboard dành cho lãnh đạo. | Chưa kiểm thử | Chưa kiểm thử | Kiểm tra một hành vi duy nhất là đăng nhập hợp lệ. |
| TC_AUTH_02 | Đăng nhập | Kiểm tra hệ thống từ chối đăng nhập khi mật khẩu không đúng. | Cán bộ thực hiện | Tài khoản `canbo01` đã tồn tại và đang hoạt động. | username: `canbo01`<br>password: `SaiMatKhau@123` | 1. Mở trang đăng nhập.<br>2. Nhập username hợp lệ và mật khẩu sai.<br>3. Nhấn nút đăng nhập.<br>4. Quan sát thông báo lỗi. | Hệ thống không cho đăng nhập, không tạo token JWT, hiển thị thông báo tài khoản hoặc mật khẩu không chính xác. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra khóa tài khoản trong test case này. |
| TC_AUTH_03 | Phân quyền theo vai trò | Kiểm tra cán bộ thực hiện không được truy cập màn hình quản trị người dùng. | Cán bộ thực hiện | Người dùng `canbo01` đã đăng nhập với vai trò `can_bo`. | URL truy cập: `/admin/users`<br>role: `can_bo` | 1. Đăng nhập bằng tài khoản `canbo01`.<br>2. Truy cập trực tiếp đường dẫn quản lý người dùng.<br>3. Quan sát phản hồi của giao diện. | Hệ thống chặn truy cập, hiển thị thông báo không có quyền hoặc điều hướng về trang phù hợp với vai trò cán bộ. | Chưa kiểm thử | Chưa kiểm thử | Use Case tập trung vào phân quyền giao diện/API theo vai trò. |
| TC_AUTH_04 | Đổi mật khẩu | Kiểm tra người dùng đổi mật khẩu thành công khi nhập đúng mật khẩu hiện tại và mật khẩu mới hợp lệ. | Văn thư | Tài khoản `vanthu01` đã đăng nhập, mật khẩu hiện tại là `Vt@123456`. | mật khẩu hiện tại: `Vt@123456`<br>mật khẩu mới: `Vt@654321`<br>xác nhận mật khẩu mới: `Vt@654321` | 1. Vào màn hình tài khoản cá nhân.<br>2. Chọn chức năng đổi mật khẩu.<br>3. Nhập mật khẩu hiện tại, mật khẩu mới và xác nhận mật khẩu mới.<br>4. Nhấn lưu thay đổi. | Hệ thống cập nhật mật khẩu thành công, hiển thị thông báo thành công và yêu cầu người dùng đăng nhập lại hoặc tiếp tục phiên theo thiết kế hệ thống. | Chưa kiểm thử | Chưa kiểm thử | Sau kiểm thử có thể cần khôi phục mật khẩu ban đầu trên môi trường test. |
| TC_AUTH_05 | Cập nhật hồ sơ cá nhân | Kiểm tra người dùng cập nhật thông tin hồ sơ cá nhân hợp lệ. | Trưởng phòng | Tài khoản `truongphong01` đã đăng nhập. | họ tên: `Nguyễn Văn Minh`<br>số điện thoại: `0912345678`<br>email: `minhnv@tamthanh.gov.vn` | 1. Mở màn hình hồ sơ cá nhân.<br>2. Cập nhật họ tên, số điện thoại và email.<br>3. Nhấn lưu.<br>4. Tải lại trang hồ sơ. | Hệ thống lưu thông tin mới, hiển thị dữ liệu đã cập nhật đúng với dữ liệu đầu vào sau khi tải lại trang. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra đổi ảnh đại diện trong test case này. |

## 2. Test case nhóm Quản trị hệ thống

| Mã test case | Use Case liên quan | Mục tiêu kiểm thử | Tác nhân thực hiện | Điều kiện tiên quyết | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|
| TC_ADMIN_01 | Quản lý người dùng | Kiểm tra quản trị viên tạo mới tài khoản người dùng cho cán bộ thực hiện. | Quản trị viên | Quản trị viên `admin01` đã đăng nhập và đơn vị `Văn phòng Đảng ủy` đã tồn tại. | username: `canbo02`<br>họ tên: `Trần Thị Hòa`<br>role: `can_bo`<br>đơn vị: `Văn phòng Đảng ủy`<br>email: `hoatt@tamthanh.gov.vn` | 1. Vào màn hình quản lý người dùng.<br>2. Chọn tạo người dùng mới.<br>3. Nhập đầy đủ thông tin tài khoản.<br>4. Nhấn lưu. | Người dùng `canbo02` được tạo thành công, hiển thị trong danh sách người dùng với vai trò `can_bo` và đơn vị đã chọn. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra gửi email kích hoạt trong test case này. |
| TC_ADMIN_02 | Quản lý vai trò và quyền hạn | Kiểm tra quản trị viên gán quyền xem nhật ký hệ thống cho vai trò văn thư. | Quản trị viên | Vai trò `van_thu` đã tồn tại trong hệ thống. | role: `van_thu`<br>quyền thêm: `view_system_logs` | 1. Vào màn hình vai trò và quyền hạn.<br>2. Chọn vai trò `van_thu`.<br>3. Bật quyền `view_system_logs`.<br>4. Lưu cấu hình quyền. | Hệ thống lưu cấu hình quyền thành công; tài khoản thuộc vai trò `van_thu` có thể nhìn thấy chức năng xem nhật ký hệ thống theo quyền mới. | Chưa kiểm thử | Chưa kiểm thử | Chỉ kiểm tra thao tác gán một quyền cụ thể. |
| TC_ADMIN_03 | Quản lý đơn vị | Kiểm tra quản trị viên tạo mới một đơn vị trực thuộc. | Quản trị viên | Quản trị viên đã đăng nhập, mã đơn vị `BAN_TUYEN_GIAO` chưa tồn tại. | tên đơn vị: `Ban Tuyên giáo Đảng ủy`<br>mã đơn vị: `BAN_TUYEN_GIAO`<br>người phụ trách: `truongphong01` | 1. Vào màn hình quản lý đơn vị.<br>2. Chọn tạo đơn vị mới.<br>3. Nhập tên, mã đơn vị và người phụ trách.<br>4. Nhấn lưu. | Đơn vị mới được tạo và xuất hiện trong danh sách đơn vị; người phụ trách được gắn đúng là `truongphong01`. | Chưa kiểm thử | Chưa kiểm thử | Dùng cho kiểm thử dữ liệu tổ chức. |
| TC_ADMIN_04 | Quản lý trạng thái nhiệm vụ | Kiểm tra quản trị viên thêm trạng thái nhiệm vụ mới dùng cho theo dõi tiến độ. | Quản trị viên | Chưa tồn tại trạng thái có mã `CHO_DUYET_CAP_DON_VI`. | tên trạng thái: `Chờ duyệt cấp đơn vị`<br>mã trạng thái: `CHO_DUYET_CAP_DON_VI`<br>màu hiển thị: `#F59E0B` | 1. Vào màn hình quản lý trạng thái nhiệm vụ.<br>2. Chọn thêm trạng thái.<br>3. Nhập tên, mã và màu hiển thị.<br>4. Nhấn lưu. | Trạng thái mới được lưu, có thể được chọn trong luồng xử lý nhiệm vụ và hiển thị đúng màu cấu hình. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra xóa trạng thái trong test case này. |
| TC_ADMIN_05 | Xem nhật ký bảo mật | Kiểm tra quản trị viên xem được nhật ký đăng nhập thất bại của người dùng. | Quản trị viên | Đã có ít nhất một lần đăng nhập thất bại của tài khoản `canbo01`. | bộ lọc loại sự kiện: `LOGIN_FAILED`<br>từ ngày: `2026-06-01`<br>đến ngày: `2026-06-30` | 1. Vào màn hình nhật ký bảo mật.<br>2. Chọn bộ lọc loại sự kiện `LOGIN_FAILED`.<br>3. Chọn khoảng thời gian tháng 6/2026.<br>4. Nhấn tìm kiếm. | Danh sách nhật ký hiển thị các bản ghi đăng nhập thất bại, gồm tài khoản liên quan, thời gian, địa chỉ IP và nội dung sự kiện. | Chưa kiểm thử | Chưa kiểm thử | Phục vụ kiểm tra khả năng truy vết bảo mật. |

## 3. Test case nhóm Quản lý văn bản

| Mã test case | Use Case liên quan | Mục tiêu kiểm thử | Tác nhân thực hiện | Điều kiện tiên quyết | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|
| TC_DOC_01 | Tiếp nhận văn bản | Kiểm tra văn thư tiếp nhận và tạo hồ sơ văn bản đến mới. | Văn thư | Văn thư `vanthu01` đã đăng nhập. | số văn bản: `128/CV-UBND`<br>ngày ban hành: `2026-06-05`<br>cơ quan ban hành: `UBND phường Tam Thanh`<br>tiêu đề: `Kế hoạch kiểm tra công tác tháng 6` | 1. Vào màn hình quản lý văn bản.<br>2. Chọn tiếp nhận văn bản đến.<br>3. Nhập thông tin văn bản.<br>4. Nhấn lưu. | Văn bản được tạo thành công với đầy đủ số văn bản, ngày ban hành, cơ quan ban hành và tiêu đề; trạng thái ban đầu là chờ bổ sung file hoặc chờ xử lý theo thiết kế. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra upload file trong test case này. |
| TC_DOC_02 | Upload file văn bản | Kiểm tra văn thư upload file PDF cho văn bản đã tiếp nhận. | Văn thư | Văn bản `128/CV-UBND` đã được tạo và chưa có file đính kèm. | file upload: `ke-hoach-thang-6.pdf`<br>loại file: `application/pdf`<br>dung lượng: `2 MB` | 1. Mở chi tiết văn bản `128/CV-UBND`.<br>2. Chọn chức năng upload file.<br>3. Chọn file `ke-hoach-thang-6.pdf`.<br>4. Nhấn tải lên. | File PDF được upload thành công, hiển thị trong danh sách tệp đính kèm của văn bản và có thể tải xuống hoặc xem lại. | Chưa kiểm thử | Chưa kiểm thử | Chỉ dùng file hợp lệ, không kiểm tra file sai định dạng. |
| TC_DOC_03 | Cập nhật văn bản | Kiểm tra văn thư cập nhật thông tin tiêu đề và trích yếu của văn bản trước khi gửi kiểm tra. | Văn thư | Văn bản `128/CV-UBND` đang ở trạng thái cho phép chỉnh sửa. | tiêu đề mới: `Kế hoạch kiểm tra công tác Đảng tháng 6`<br>trích yếu: `Kiểm tra tiến độ thực hiện các nhiệm vụ trọng tâm trong tháng 6/2026` | 1. Mở chi tiết văn bản cần cập nhật.<br>2. Chọn chỉnh sửa thông tin.<br>3. Cập nhật tiêu đề và trích yếu.<br>4. Nhấn lưu. | Hệ thống lưu thông tin mới, lịch sử cập nhật ghi nhận người sửa là `vanthu01` và thời điểm chỉnh sửa. | Chưa kiểm thử | Chưa kiểm thử | Không thay đổi file đính kèm trong test case này. |
| TC_DOC_04 | Gửi văn bản đến Văn phòng Đảng ủy | Kiểm tra văn thư gửi văn bản đã đủ thông tin sang Văn phòng Đảng ủy để kiểm tra. | Văn thư | Văn bản đã có thông tin bắt buộc và file `ke-hoach-thang-6.pdf`. | mã văn bản: `128/CV-UBND`<br>người nhận xử lý: `chanhvanphong01` | 1. Mở chi tiết văn bản.<br>2. Chọn gửi đến Văn phòng Đảng ủy.<br>3. Chọn người nhận `chanhvanphong01` nếu hệ thống yêu cầu.<br>4. Xác nhận gửi. | Văn bản chuyển sang trạng thái chờ kiểm tra; Chánh văn phòng nhận được thông báo có văn bản mới cần kiểm tra. | Chưa kiểm thử | Chưa kiểm thử | Kiểm tra một bước chuyển trạng thái cụ thể. |
| TC_DOC_05 | Chánh văn phòng kiểm tra văn bản | Kiểm tra Chánh văn phòng xác nhận văn bản hợp lệ sau khi kiểm tra nội dung. | Chánh văn phòng | Văn bản `128/CV-UBND` đang ở trạng thái chờ kiểm tra. | kết luận kiểm tra: `Hợp lệ`<br>ghi chú: `Văn bản đủ thông tin và đúng file đính kèm` | 1. Đăng nhập bằng `chanhvanphong01`.<br>2. Mở danh sách văn bản chờ kiểm tra.<br>3. Chọn văn bản `128/CV-UBND`.<br>4. Chọn xác nhận hợp lệ và nhập ghi chú. | Văn bản được đánh dấu đã kiểm tra hợp lệ, trạng thái chuyển sang chờ duyệt hoặc chờ trình lãnh đạo theo quy trình. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra trường hợp yêu cầu bổ sung ở test case này. |
| TC_DOC_06 | Yêu cầu bổ sung văn bản | Kiểm tra Chánh văn phòng yêu cầu văn thư bổ sung khi văn bản thiếu thông tin. | Chánh văn phòng | Văn bản `129/CV-UBND` đang ở trạng thái chờ kiểm tra và thiếu file đính kèm. | lý do bổ sung: `Văn bản chưa có file PDF scan bản ký`<br>người nhận yêu cầu: `vanthu01` | 1. Đăng nhập bằng `chanhvanphong01`.<br>2. Mở văn bản `129/CV-UBND`.<br>3. Chọn yêu cầu bổ sung.<br>4. Nhập lý do và xác nhận gửi yêu cầu. | Văn bản chuyển sang trạng thái yêu cầu bổ sung; văn thư nhận thông báo nêu rõ lý do cần bổ sung file PDF scan bản ký. | Chưa kiểm thử | Chưa kiểm thử | Use Case riêng cho nhánh không hợp lệ. |

## 4. Test case nhóm Quản lý nhiệm vụ và phần việc

| Mã test case | Use Case liên quan | Mục tiêu kiểm thử | Tác nhân thực hiện | Điều kiện tiên quyết | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|
| TC_TASK_01 | Tạo nhiệm vụ trực tiếp | Kiểm tra lãnh đạo tạo nhiệm vụ trực tiếp không gắn văn bản. | Lãnh đạo Đảng ủy | Lãnh đạo `lanhdao01` đã đăng nhập. | tiêu đề nhiệm vụ: `Rà soát chỉ tiêu phát triển Đảng viên tháng 6`<br>mô tả: `Tổng hợp số liệu và đề xuất giải pháp hoàn thành chỉ tiêu`<br>hạn chót: `2026-06-30`<br>mức ưu tiên: `Cao` | 1. Vào màn hình nhiệm vụ.<br>2. Chọn tạo nhiệm vụ mới.<br>3. Nhập thông tin nhiệm vụ trực tiếp.<br>4. Nhấn lưu. | Nhiệm vụ được tạo thành công, có mã nhiệm vụ mới, trạng thái ban đầu là mới tạo hoặc chờ giao đơn vị theo quy trình hệ thống. | Chưa kiểm thử | Chưa kiểm thử | Không gắn văn bản nguồn trong test case này. |
| TC_TASK_02 | Tạo nhiệm vụ từ văn bản | Kiểm tra Chánh văn phòng tạo nhiệm vụ từ văn bản đã được kiểm tra hợp lệ. | Chánh văn phòng | Văn bản `128/CV-UBND` đã hợp lệ và có thể tạo nhiệm vụ. | văn bản nguồn: `128/CV-UBND`<br>tiêu đề nhiệm vụ: `Triển khai kế hoạch kiểm tra công tác tháng 6`<br>hạn chót: `2026-06-30` | 1. Mở chi tiết văn bản `128/CV-UBND`.<br>2. Chọn tạo nhiệm vụ từ văn bản.<br>3. Kiểm tra thông tin nhiệm vụ được lấy từ văn bản.<br>4. Bổ sung hạn chót và nhấn tạo. | Nhiệm vụ mới được tạo và liên kết với văn bản nguồn; trong chi tiết nhiệm vụ hiển thị đúng số văn bản `128/CV-UBND`. | Chưa kiểm thử | Chưa kiểm thử | Kiểm tra liên kết nhiệm vụ với văn bản. |
| TC_TASK_03 | Giao nhiệm vụ cho đơn vị | Kiểm tra lãnh đạo giao một nhiệm vụ cho đơn vị phụ trách cụ thể. | Lãnh đạo Đảng ủy | Nhiệm vụ `Triển khai kế hoạch kiểm tra công tác tháng 6` đã tồn tại và chưa giao đơn vị. | đơn vị nhận: `Ban Tuyên giáo Đảng ủy`<br>người phụ trách: `truongphong01`<br>hạn chót đơn vị: `2026-06-28` | 1. Mở chi tiết nhiệm vụ.<br>2. Chọn giao nhiệm vụ cho đơn vị.<br>3. Chọn đơn vị và người phụ trách.<br>4. Nhập hạn chót của đơn vị và xác nhận giao. | Nhiệm vụ được giao cho `Ban Tuyên giáo Đảng ủy`; trưởng phòng phụ trách nhận thông báo nhiệm vụ mới cần tiếp nhận. | Chưa kiểm thử | Chưa kiểm thử | Chỉ kiểm tra giao cho một đơn vị. |
| TC_TASK_04 | Trưởng phòng xác nhận tiếp nhận nhiệm vụ | Kiểm tra trưởng phòng xác nhận đã tiếp nhận nhiệm vụ được lãnh đạo giao. | Trưởng phòng | Nhiệm vụ đã được giao cho đơn vị của `truongphong01` và đang chờ tiếp nhận. | mã nhiệm vụ: `TASK-2026-06-001`<br>hành động: `Xác nhận tiếp nhận` | 1. Đăng nhập bằng `truongphong01`.<br>2. Mở danh sách nhiệm vụ chờ tiếp nhận.<br>3. Chọn nhiệm vụ `TASK-2026-06-001`.<br>4. Nhấn xác nhận tiếp nhận. | Nhiệm vụ chuyển sang trạng thái đơn vị đã tiếp nhận; thời điểm tiếp nhận và người tiếp nhận được ghi nhận. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra từ chối tiếp nhận trong test case này. |
| TC_TASK_05 | Phân công cán bộ thực hiện | Kiểm tra trưởng phòng phân công cán bộ phụ trách nhiệm vụ trong đơn vị. | Trưởng phòng | Nhiệm vụ `TASK-2026-06-001` đã được đơn vị tiếp nhận. | cán bộ được phân công: `canbo01`<br>vai trò xử lý: `Chủ trì`<br>ghi chú phân công: `Tổng hợp số liệu từ các chi bộ` | 1. Mở chi tiết nhiệm vụ của đơn vị.<br>2. Chọn phân công cán bộ.<br>3. Chọn `canbo01` và vai trò chủ trì.<br>4. Nhấn lưu phân công. | Cán bộ `canbo01` được gắn vào nhiệm vụ với vai trò chủ trì và nhận thông báo có nhiệm vụ mới. | Chưa kiểm thử | Chưa kiểm thử | Chỉ phân công một cán bộ chủ trì. |
| TC_TASK_06 | Tạo phần việc | Kiểm tra trưởng phòng tạo phần việc cụ thể thuộc nhiệm vụ đã tiếp nhận. | Trưởng phòng | Nhiệm vụ `TASK-2026-06-001` đã có cán bộ `canbo01` được phân công. | tên phần việc: `Thu thập số liệu kiểm tra tháng 6`<br>người thực hiện: `canbo01`<br>hạn chót: `2026-06-20`<br>mô tả: `Tổng hợp số liệu từ các tổ chức cơ sở Đảng` | 1. Mở chi tiết nhiệm vụ.<br>2. Chọn thêm phần việc.<br>3. Nhập tên, mô tả, người thực hiện và hạn chót.<br>4. Nhấn lưu. | Phần việc được tạo trong nhiệm vụ, gán cho `canbo01` và có trạng thái ban đầu là chưa bắt đầu. | Chưa kiểm thử | Chưa kiểm thử | Kiểm tra phần việc con, không kiểm tra nhiệm vụ tổng. |
| TC_TASK_07 | Cán bộ bắt đầu thực hiện phần việc | Kiểm tra cán bộ chuyển phần việc được giao sang trạng thái đang thực hiện. | Cán bộ thực hiện | Phần việc `Thu thập số liệu kiểm tra tháng 6` đã được giao cho `canbo01`. | hành động: `Bắt đầu thực hiện`<br>ngày bắt đầu: `2026-06-12` | 1. Đăng nhập bằng `canbo01`.<br>2. Mở danh sách phần việc được giao.<br>3. Chọn phần việc cần xử lý.<br>4. Nhấn bắt đầu thực hiện. | Phần việc chuyển sang trạng thái đang thực hiện; hệ thống ghi nhận ngày bắt đầu là `2026-06-12`. | Chưa kiểm thử | Chưa kiểm thử | Không cập nhật tiến độ trong test case này. |
| TC_TASK_08 | Cập nhật tiến độ | Kiểm tra cán bộ cập nhật phần trăm tiến độ và nội dung xử lý cho phần việc đang thực hiện. | Cán bộ thực hiện | Phần việc đang ở trạng thái đang thực hiện. | tiến độ: `60%`<br>nội dung cập nhật: `Đã nhận số liệu từ 4/6 chi bộ`<br>ngày cập nhật: `2026-06-18` | 1. Mở chi tiết phần việc.<br>2. Chọn cập nhật tiến độ.<br>3. Nhập tiến độ 60% và nội dung xử lý.<br>4. Nhấn lưu cập nhật. | Tiến độ phần việc hiển thị 60%, lịch sử xử lý ghi nhận nội dung cập nhật và người cập nhật là `canbo01`. | Chưa kiểm thử | Chưa kiểm thử | Không nộp kết quả cuối cùng trong test case này. |

## 5. Test case nhóm Phê duyệt nhiệm vụ

| Mã test case | Use Case liên quan | Mục tiêu kiểm thử | Tác nhân thực hiện | Điều kiện tiên quyết | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|
| TC_APPROVAL_01 | Trưởng phòng duyệt phần việc cấp đơn vị | Kiểm tra trưởng phòng duyệt phần việc sau khi cán bộ nộp kết quả và minh chứng. | Trưởng phòng | Cán bộ `canbo01` đã nộp kết quả phần việc với file minh chứng. | phần việc: `Thu thập số liệu kiểm tra tháng 6`<br>file minh chứng: `tong-hop-so-lieu-thang-6.xlsx`<br>ý kiến duyệt: `Số liệu đầy đủ, đồng ý duyệt` | 1. Đăng nhập bằng `truongphong01`.<br>2. Mở danh sách phần việc chờ duyệt cấp đơn vị.<br>3. Xem kết quả và file minh chứng.<br>4. Chọn duyệt và nhập ý kiến. | Phần việc chuyển sang trạng thái đã duyệt cấp đơn vị; cán bộ thực hiện nhận thông báo kết quả duyệt. | Chưa kiểm thử | Chưa kiểm thử | Không chuyển lên lãnh đạo trong test case này. |
| TC_APPROVAL_02 | Trưởng phòng yêu cầu cán bộ chỉnh sửa | Kiểm tra trưởng phòng trả lại phần việc khi minh chứng chưa đạt yêu cầu. | Trưởng phòng | Phần việc đang chờ duyệt cấp đơn vị. | lý do chỉnh sửa: `File minh chứng thiếu số liệu của Chi bộ 5 và Chi bộ 6`<br>người nhận: `canbo01` | 1. Mở phần việc đang chờ duyệt.<br>2. Kiểm tra nội dung kết quả và file minh chứng.<br>3. Chọn yêu cầu chỉnh sửa.<br>4. Nhập lý do và xác nhận gửi. | Phần việc chuyển sang trạng thái cần chỉnh sửa; cán bộ `canbo01` nhận thông báo kèm lý do cụ thể. | Chưa kiểm thử | Chưa kiểm thử | Use Case riêng cho nhánh không đạt cấp đơn vị. |
| TC_APPROVAL_03 | Trưởng phòng chuyển nhiệm vụ lên lãnh đạo phê duyệt | Kiểm tra trưởng phòng gửi nhiệm vụ đã hoàn thành cấp đơn vị lên lãnh đạo phê duyệt. | Trưởng phòng | Tất cả phần việc thuộc nhiệm vụ `TASK-2026-06-001` đã được duyệt cấp đơn vị. | mã nhiệm vụ: `TASK-2026-06-001`<br>người phê duyệt: `lanhdao01`<br>ghi chú: `Đơn vị đã hoàn thành và gửi lãnh đạo phê duyệt` | 1. Mở chi tiết nhiệm vụ.<br>2. Kiểm tra danh sách phần việc đã duyệt.<br>3. Chọn chuyển lãnh đạo phê duyệt.<br>4. Nhập ghi chú và xác nhận. | Nhiệm vụ chuyển sang trạng thái chờ lãnh đạo phê duyệt; lãnh đạo `lanhdao01` nhận thông báo có nhiệm vụ chờ duyệt. | Chưa kiểm thử | Chưa kiểm thử | Kiểm tra điều kiện đủ phần việc đã duyệt. |
| TC_APPROVAL_04 | Lãnh đạo phê duyệt nhiệm vụ | Kiểm tra lãnh đạo phê duyệt hoàn thành nhiệm vụ sau khi đơn vị gửi lên. | Lãnh đạo Đảng ủy | Nhiệm vụ `TASK-2026-06-001` đang chờ lãnh đạo phê duyệt. | kết quả phê duyệt: `Đồng ý`<br>ý kiến lãnh đạo: `Thống nhất kết quả thực hiện của đơn vị` | 1. Đăng nhập bằng `lanhdao01`.<br>2. Mở danh sách nhiệm vụ chờ phê duyệt.<br>3. Xem chi tiết kết quả và minh chứng.<br>4. Chọn phê duyệt hoàn thành. | Nhiệm vụ chuyển sang trạng thái hoàn thành; hệ thống ghi nhận ý kiến phê duyệt của lãnh đạo và thời điểm phê duyệt. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra yêu cầu đơn vị chỉnh sửa trong test case này. |

## 6. Test case nhóm Báo cáo tháng

| Mã test case | Use Case liên quan | Mục tiêu kiểm thử | Tác nhân thực hiện | Điều kiện tiên quyết | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|
| TC_REPORT_01 | Hệ thống nhắc báo cáo tháng | Kiểm tra hệ thống tạo thông báo nhắc gửi báo cáo tháng cho cán bộ. | Hệ thống | Đến thời điểm nhắc báo cáo tháng 6/2026 theo cấu hình hệ thống. | kỳ báo cáo: `2026-06`<br>người nhận: `canbo01`<br>hạn gửi: `2026-06-25` | 1. Đến ngày hệ thống cấu hình nhắc báo cáo.<br>2. Đăng nhập bằng `canbo01`.<br>3. Mở danh sách thông báo.<br>4. Kiểm tra thông báo nhắc báo cáo tháng. | Cán bộ nhận được thông báo yêu cầu gửi báo cáo tháng 6/2026, hiển thị rõ hạn gửi `2026-06-25`. | Chưa kiểm thử | Chưa kiểm thử | Có thể kiểm tra bằng dữ liệu giả lập trên môi trường test. |
| TC_REPORT_02 | Cán bộ gửi báo cáo tháng cá nhân | Kiểm tra cán bộ gửi báo cáo tháng cá nhân với nội dung và file minh chứng. | Cán bộ thực hiện | Cán bộ `canbo01` đã đăng nhập và kỳ báo cáo `2026-06` đang mở. | kỳ báo cáo: `2026-06`<br>nội dung: `Hoàn thành 3/4 phần việc được giao trong tháng 6`<br>file đính kèm: `bao-cao-ca-nhan-thang-6.docx` | 1. Mở màn hình báo cáo tháng cá nhân.<br>2. Chọn kỳ báo cáo tháng 6/2026.<br>3. Nhập nội dung báo cáo và đính kèm file.<br>4. Nhấn gửi báo cáo. | Báo cáo cá nhân được gửi thành công, trạng thái chuyển sang đã gửi và trưởng phòng phụ trách có thể xem báo cáo. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra sửa báo cáo sau khi gửi. |
| TC_REPORT_03 | Trưởng phòng tổng hợp báo cáo tháng đơn vị | Kiểm tra trưởng phòng tổng hợp báo cáo tháng từ các báo cáo cá nhân trong đơn vị. | Trưởng phòng | Các cán bộ trong đơn vị đã gửi báo cáo cá nhân tháng 6/2026. | kỳ báo cáo: `2026-06`<br>đơn vị: `Ban Tuyên giáo Đảng ủy`<br>nhận xét: `Đơn vị hoàn thành 85% nhiệm vụ trong tháng` | 1. Đăng nhập bằng `truongphong01`.<br>2. Mở màn hình tổng hợp báo cáo đơn vị.<br>3. Chọn kỳ tháng 6/2026.<br>4. Kiểm tra dữ liệu tự tổng hợp, nhập nhận xét và gửi báo cáo đơn vị. | Báo cáo tháng của đơn vị được tạo từ dữ liệu cán bộ, lưu nhận xét của trưởng phòng và chuyển sang trạng thái đã gửi lãnh đạo. | Chưa kiểm thử | Chưa kiểm thử | Kiểm tra tổng hợp ở cấp đơn vị. |
| TC_REPORT_04 | Xuất báo cáo Excel | Kiểm tra lãnh đạo xuất báo cáo tháng các đơn vị ra file Excel. | Lãnh đạo Đảng ủy | Có dữ liệu báo cáo đơn vị cho kỳ `2026-06`. | kỳ báo cáo: `2026-06`<br>định dạng xuất: `xlsx`<br>tên file mong đợi: `bao-cao-thang-6-2026.xlsx` | 1. Đăng nhập bằng `lanhdao01`.<br>2. Mở màn hình báo cáo tháng các đơn vị.<br>3. Chọn kỳ báo cáo tháng 6/2026.<br>4. Nhấn xuất Excel. | Hệ thống tạo file Excel `bao-cao-thang-6-2026.xlsx` chứa danh sách đơn vị, số nhiệm vụ, tỷ lệ hoàn thành và nhận xét tổng hợp. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra nội dung định dạng nâng cao của file Excel. |

## 7. Test case nhóm Thông báo, trao đổi nội bộ, dashboard và KPI

| Mã test case | Use Case liên quan | Mục tiêu kiểm thử | Tác nhân thực hiện | Điều kiện tiên quyết | Dữ liệu đầu vào | Các bước thực hiện | Kết quả mong đợi | Kết quả thực tế | Trạng thái | Ghi chú |
|---|---|---|---|---|---|---|---|---|---|---|
| TC_NOTIFY_01 | Xem danh sách thông báo | Kiểm tra người dùng xem danh sách thông báo liên quan đến tài khoản của mình. | Trưởng phòng | Tài khoản `truongphong01` có ít nhất một thông báo nhiệm vụ mới. | tài khoản: `truongphong01`<br>loại thông báo: `TASK_ASSIGNED` | 1. Đăng nhập bằng `truongphong01`.<br>2. Mở biểu tượng hoặc màn hình thông báo.<br>3. Quan sát danh sách thông báo. | Hệ thống hiển thị danh sách thông báo của `truongphong01`, gồm nội dung, thời gian tạo và trạng thái đã đọc/chưa đọc. | Chưa kiểm thử | Chưa kiểm thử | Không kiểm tra đánh dấu đã đọc trong test case này. |
| TC_NOTIFY_02 | Đánh dấu thông báo đã đọc | Kiểm tra người dùng đánh dấu một thông báo chưa đọc thành đã đọc. | Trưởng phòng | Có thông báo chưa đọc với nội dung `Bạn có nhiệm vụ mới cần tiếp nhận`. | mã thông báo: `NOTI-2026-06-001`<br>hành động: `Đánh dấu đã đọc` | 1. Mở danh sách thông báo.<br>2. Chọn thông báo `NOTI-2026-06-001`.<br>3. Nhấn đánh dấu đã đọc.<br>4. Kiểm tra trạng thái thông báo. | Thông báo chuyển sang trạng thái đã đọc; số lượng thông báo chưa đọc giảm đi 1. | Chưa kiểm thử | Chưa kiểm thử | Liên quan trực tiếp đến bộ đếm chưa đọc. |
| TC_NOTIFY_03 | Tạo hội thoại theo nhiệm vụ | Kiểm tra hệ thống tạo hội thoại trao đổi nội bộ gắn với một nhiệm vụ cụ thể. | Trưởng phòng | Nhiệm vụ `TASK-2026-06-001` đã tồn tại và có người tham gia `truongphong01`, `canbo01`. | mã nhiệm vụ: `TASK-2026-06-001`<br>thành viên hội thoại: `truongphong01`, `canbo01` | 1. Mở chi tiết nhiệm vụ.<br>2. Chọn tab hoặc chức năng trao đổi.<br>3. Chọn tạo hội thoại nhiệm vụ nếu chưa có.<br>4. Kiểm tra danh sách thành viên. | Hội thoại được tạo và gắn đúng với nhiệm vụ; chỉ các thành viên liên quan đến nhiệm vụ nhìn thấy hội thoại này. | Chưa kiểm thử | Chưa kiểm thử | Không gửi tin nhắn trong test case này. |
| TC_NOTIFY_04 | Gửi tin nhắn trong hội thoại nhiệm vụ | Kiểm tra cán bộ gửi tin nhắn realtime trong hội thoại của nhiệm vụ. | Cán bộ thực hiện | Hội thoại của nhiệm vụ `TASK-2026-06-001` đã tồn tại; `canbo01` và `truongphong01` đang có quyền truy cập. | nội dung tin nhắn: `Em đã cập nhật tiến độ và bổ sung file minh chứng mới.`<br>người gửi: `canbo01` | 1. Đăng nhập bằng `canbo01`.<br>2. Mở hội thoại của nhiệm vụ `TASK-2026-06-001`.<br>3. Nhập nội dung tin nhắn.<br>4. Nhấn gửi và quan sát phía người nhận. | Tin nhắn được lưu trong hội thoại; người nhận `truongphong01` thấy tin nhắn mới gần như ngay lập tức qua Socket.IO. | Chưa kiểm thử | Chưa kiểm thử | Có thể kiểm tra bằng hai trình duyệt hoặc hai tài khoản test. |
| TC_NOTIFY_05 | Xem dashboard theo vai trò và KPI | Kiểm tra lãnh đạo xem dashboard tổng quan nhiệm vụ, KPI và nhiệm vụ quá hạn. | Lãnh đạo Đảng ủy | Có dữ liệu nhiệm vụ của nhiều đơn vị trong tháng 6/2026. | tài khoản: `lanhdao01`<br>khoảng thời gian: `2026-06-01` đến `2026-06-30`<br>vai trò: `lanh_dao` | 1. Đăng nhập bằng `lanhdao01`.<br>2. Mở dashboard.<br>3. Chọn khoảng thời gian tháng 6/2026.<br>4. Quan sát thống kê nhiệm vụ, KPI và danh sách quá hạn. | Dashboard hiển thị đúng dữ liệu theo vai trò lãnh đạo, gồm số nhiệm vụ theo trạng thái, thống kê theo đơn vị, KPI tổng quan, bảng xếp hạng KPI và danh sách nhiệm vụ quá hạn. | Chưa kiểm thử | Chưa kiểm thử | Test case bao phủ hành vi xem dashboard lãnh đạo, không chỉnh sửa dữ liệu. |
